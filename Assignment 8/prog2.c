//WAP to concatinate the 2 string
#include<stdio.h>
void main()
{
    int k,i,j,c=0;
    char str1[50],str2[50],str[100];
    printf("Enter the string: \n");
    gets(str1);
    printf("Enter the string: \n");
    gets(str2);
    for ( i = 0; str1[i]!='\0'; i++);
    for ( j = 0; str2[i]!='\0'; j++);
    for(k=0;k<(i+j);k++)
    {
       //
    }
}